class CoinUseCases {
/*   final CoinRepository _coinRepository;

  CoinUseCases(this._coinRepository);

  Future<Either<Failure, List<Coin>>> getCoins() async {
    return await _coinRepository.getCoins();
  }

  Future<Either<Failure, Coin>> getCoin(String id) async {
    return await _coinRepository.getCoin(id);
  }

  Future<Either<Failure, List<Coin>>> searchCoins(String query) async {
    return await _coinRepository.searchCoins(query);
  } */
}
